# 07 — Generative AI

Working with large language models, fine-tuning, embeddings, and multimodal systems.

## Contents

| Folder | Topics |
|--------|--------|
| `01_llm_fundamentals/` | How LLMs work, tokenisation, context windows, inference parameters |
| `02_fine_tuning/` | LoRA, QLoRA, PEFT, instruction tuning, RLHF concepts |
| `03_embeddings/` | Embedding models, semantic similarity, sentence-transformers |
| `04_multimodal/` | Vision-language models, image generation, audio/video models |

## Setup
```bash
python -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
jupyter lab
```