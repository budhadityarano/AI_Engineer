# 04 — Data Science (NumPy · Pandas · EDA)

The data wrangling and exploration toolkit every AI Engineer must own.

## Contents

| Folder | Topics |
|--------|--------|
| `01_numpy/` | Arrays, broadcasting, linear algebra ops, random, performance tips |
| `02_pandas/` | Series/DataFrame, merge, groupby, time series, apply, optimisation |
| `03_eda/` | Univariate & bivariate analysis, missing data, outliers, feature insights |

## Setup
```bash
python -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
jupyter lab
```