# 03 — SQL

From SELECT basics to window functions and query optimisation — all topics asked in AI/DS interviews.

## Contents

| Folder | Topics |
|--------|--------|
| `01_basics/` | SELECT, WHERE, GROUP BY, HAVING, ORDER BY |
| `02_joins_aggregations/` | INNER/LEFT/RIGHT/FULL joins, subqueries, CTEs |
| `03_window_functions/` | ROW_NUMBER, RANK, LAG/LEAD, running totals |
| `04_query_optimization/` | Indexes, EXPLAIN, partitioning, execution plans |
| `05_practice_problems/` | 50+ interview-style SQL problems with solutions |

## Setup
```bash
python -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
jupyter lab
```